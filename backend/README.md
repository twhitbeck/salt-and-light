# Salt and Light backend server

This is an [Express](https://expressjs.com/) based api server.

## Routes

You'll find the routes defined in `./routes/api.ts`

## Database

Fetching data from the database happens in the `./db.ts` module.
